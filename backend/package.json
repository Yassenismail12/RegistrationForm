{
  "name": "yly-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.202",
    "typescript": "^5.4.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "firebase": "^12.14.0"
  }
}
