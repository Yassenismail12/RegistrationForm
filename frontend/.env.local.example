# Cloudflare Pages frontend environment variables
NEXT_PUBLIC_API_URL=https://<your-worker-subdomain>.workers.dev

# Admin dashboard is at /admin — set ADMIN_PASSWORD on the Worker (not here):
#   npx wrangler secret put ADMIN_PASSWORD
