# Frontend (Next.js)

Install and run the Next.js frontend:

```bash
cd frontend
npm install
npm run dev
```

The form will be available at http://localhost:3000. Configure the backend URL with `NEXT_PUBLIC_API_URL` (defaults to http://localhost:3001).
